# Accéder à l'app

Exécuter la commande suivante :
```bash
docker-compose up --build -d
```

Le serveur Symfony sera accessible sur :
```bash
http://localhost:8000
```

Et phpMyAdmin ici :
```bash
http://localhost:8080
```